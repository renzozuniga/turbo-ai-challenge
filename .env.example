# Used by docker-compose to provision the local Postgres container.
# Copy this file to `.env` and adjust if needed:
#   cp .env.example .env
POSTGRES_DB=turbo_ai_challenge
POSTGRES_USER=turbo
POSTGRES_PASSWORD=turbo
POSTGRES_PORT=5433
